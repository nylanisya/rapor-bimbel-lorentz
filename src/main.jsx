import React from "react";
import ReactDOM from "react-dom/client";
import RaporBimbelLorentz from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RaporBimbelLorentz />
  </React.StrictMode>
);
